# Build-check dependency (runs in CI regardless of your chosen target).
pytest>=7.0

# Our build target (#15 URL shortener): a local Flask web app.
flask>=3.0

# Optional AI-assistant track (not used by our target):
#   openai>=1.0.0
